
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# bv (beads viewer) local config and caches
.bv/

# Machine-local Claude Code permissions (not shared across machines)
.claude/settings.local.json

# Per-scenario screenshot output (not committed; may be published separately)
test/screenshots/
.DS_Store
