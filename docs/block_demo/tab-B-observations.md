# Tab B observations — B-right-stem-3row

> Expected geometry: [docs/block_demo/spec.md#tab-b--b-right-stem-3row](spec.md#tab-b--b-right-stem-3row)

## Observed (2026-08-13, corrected grid)

Grid: 181 cols × 57 rows.

- **Block 1**: header rows 1–3. Content rows 4–15. Footer rows 16–17.
- **Block 2**: header rows 20–22. Content rows 23–28. Footer rows 29–30.
- Right elbow image at cols 176–180 (ELBOW_W=5, rightmost cols). Grid confirms 5-col-wide elbow at right edge.
- Stem col 180 (far right, col bw-1) is a narrow periwinkle strip through content rows. Visible as the last cell on each row.
- Left end cap: a small rounded 2-cell-wide piece visible at the far left of bar rows (col 0–1). This is the left hcap/vcap — appears present.
- Chips on h0+2 start at col 0–1 (left side), then bar continues to the right. Chips: `main ~/code AWS` (block 1), `AWS feat/auth` (block 2).
- Footer bars span cols 0–180 with right-stem: left end is the stem cell (col 0 is periwinkle/hcap), bar runs rightward, terminates at col ~180.
- Content text starts at col 0 (no left stem, text fills from col 0). Stem is at right.
- The right-side elbow image pieces on content rows are visible as small periwinkle blocks at the right edge (col ~175–180), scattered — these are the per-row stem highlight cells showing through.

## Known open issues

- Same bar_w / right cap issue applies to the right vcap on the left end of the bar for B (the "left" end cap of a right-stem block is still a vcap variant).
- Left end cap geometry (specifically whether it's a vcap or hcap, and its width) needs confirmation.

## User feedback

_(to be filled in)_

## Fix instructions

_(to be filled in after user feedback)_
